- hosts:
    - CLOUDSVRRACPRE01
  tasks:
    - name: Ejecutar el script
      command: cd /home/oksmart/scripts && sudo ./system_info.sh
      register: info
      
    - debug: msg="{{ info.stdout }}"
    
    - debug: msg="{{ info.stdout }}"
