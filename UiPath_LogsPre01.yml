- hosts:
    - CLOUDSVRRACPRE01
  tasks:
    - name: Eliminando logs de *master-data*
      command: 'sudo rm -rf /var/apps/erp-auto/logs/*master-data.log.*'
      become: yes
      become_user: root
